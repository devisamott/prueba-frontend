import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.patners',
  appName: 'patners',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
